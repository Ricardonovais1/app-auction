<!DOCTYPE html>
<html>
  <head>
    <title>AppAuction</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
   
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <header>
      <div class="mx-auto p-4" style="width: 75%">

        <nav class="navbar navbar-expand-lg bg-body-tertiary">
          <div class="container-fluid">
            <h2><%= link_to 'Leilões de Estoque', root_path, class: 'navbar-brand text-success' %></h2>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                  <%= link_to 'Home', root_path, class: 'nav-link', 'aria-current': 'page' %>
                </li>
                <li class="nav-item">
                  <%= link_to 'Produtos cadastrados', items_path, class: 'nav-link', 'aria-current': 'page' %>
                </li>
                <% if user_signed_in? %>
                  <% if current_user.admin? %>
                    <li class="nav-item dropdown">
                      <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Administrativo
                      </a>
                  <ul class="dropdown-menu">
                    <li><%= link_to 'Categorias de produto', new_product_category_path, class: 'dropdown-item' %></li>
                    <li><%= link_to 'Criar lote', new_lot_path, class: 'dropdown-item' %></li>
                    <li><%= link_to 'Novo produto', new_item_path, class: 'dropdown-item' %></li>
                    <li><%= link_to 'Lotes pendentes',pending_lots_path, class: 'dropdown-item' %></li>
                    <li><%= link_to 'Lotes expirados', expired_lots_path, class: 'dropdown-item' %></li>
                  </ul>
                </li> 
                  <% end %>
                  <li class="sair nav-item nav-link ms-auto">
                    <%= link_to 'Sair', destroy_user_session_path, 'data-turbo-method': :delete %>
                  </li>
                  <li class="nav-item nav-link link-success">
                    <%= current_user.email %>
                  </li>
                <% else %>
                  <li class="nav-item nav-link">
                    <%= link_to 'Entrar/ Cadastrar', new_user_session_path %>
                  </li> 
                <% end %>
                   
              </ul>
              <form class="d-flex" role="search">
                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
              </form>
            </div>
          </div>
      </nav>
      <div>
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
      </div>
    </div>
  </header>
  <body>
    <main>
      <%= yield %>
    </main> 
  </body>
</html>





