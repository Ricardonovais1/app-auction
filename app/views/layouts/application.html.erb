<!DOCTYPE html>
<html>
  <head>
    <title>AppAuction</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application" %>
    <%= stylesheet_link_tag 'custom' %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <header>

      <div class="mx-auto p-4" style="max-width: 75%">

        <%= render 'navbar' %>

      <div>
        <% if notice %>
          <br /><p class="alert alert-success" role="alert"><%= notice %></p>
        <% end %>
        <% if alert %>
          <br /><p class="alert alert-warning" role="alert"><%= alert %></p>
        <% end %>
      </div>
    </div>
  </header>

  <body>
    <head>
      <%= yield :head %>
    </head>
    <main class="mx-auto p-4 bg-success bg-opacity-10 border border-3 border-success p-2 mb-2 border-opacity-50 mt-4" style="width: 75%; background: #e9ecef">
      <%= yield %>
    </main>
  </body>
</html>





